# ZApp Solana buffer-layout compatibility package

Bounds-safe local replacement for the archived `@solana/buffer-layout-utils@0.3.0`. It preserves the public layouts used by SPL Token while using JavaScript `BigInt` conversions and carrying no `bigint-buffer` native-addon dependency.
